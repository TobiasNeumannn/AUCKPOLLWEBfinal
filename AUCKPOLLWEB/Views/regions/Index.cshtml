@model IEnumerable<AUCKPOLLWEB.Models.regions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.region_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.region_pop)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.region_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.region_pop)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.regionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.regionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.regionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
